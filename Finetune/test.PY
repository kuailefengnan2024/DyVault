import torch
print(torch.cuda.is_available())  # Should return True
print(torch.cuda.current_device())  # Should return 0 (or your GPU index)
print(torch.cuda.get_device_name(0))  # Should show "NVIDIA GeForce RTX 4060 Ti"